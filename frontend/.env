# API base URL used by every request in the frontend.
# Change this in one place and every fetch / axios call picks it up.
# Example for ngrok: https://abcd-1234.ngrok-free.app
VITE_API_BASE_URL=http://127.0.0.1:3000
